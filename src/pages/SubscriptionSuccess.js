import Button from "../Button/Button";
import { Link, NavLink } from "react-router-dom";

export default function SubscriptionSuccess({ successUrl }) {
  return (
    <div>
      <Link>
        <Button></Button>
      </Link>
    </div>
  );
}
